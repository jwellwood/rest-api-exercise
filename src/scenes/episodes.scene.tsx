import * as React from 'react';
import { AppLayout } from 'layouts';
import EpisodesContainer from 'pods/episodes/episodes.container';

export const EpisodeScene = () => (
  <AppLayout>
    <EpisodesContainer />
  </AppLayout>
);
