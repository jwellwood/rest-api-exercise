import * as React from 'react';
import { AppLayout } from 'layouts';
import { CommentFormContainer } from 'pods/comment/comment-form.container';

export const CommentFormScene = () => (
  <AppLayout>
    <CommentFormContainer />
  </AppLayout>
);
